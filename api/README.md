# [Loose touch back-end](http://www.loosetouch.com)

# Run the application locally.
You can run the project with the command : `mvn spring-boot:run -Dspring-boot.run.arguments=--start-local-dynamodb -Dspring-boot.run.profiles=local-dynamodb`.

You can package the project with the command `mvn package`.
